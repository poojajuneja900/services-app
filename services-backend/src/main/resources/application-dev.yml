spring:
  jpa:
    hibernate:
      ddl-auto: update   # auto-create/update tables in dev
    show-sql: true

logging:
  level:
    com.example.serviceapp: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
